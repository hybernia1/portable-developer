<Window x:Class="PortableDeveloper.App.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:PortableDeveloper.App.Controls"
        Width="500" SizeToContent="Height" MinHeight="220" MaxHeight="540"
        ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Icon="Assets/portable-developer.ico">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <controls:DialogHeader x:Name="DialogHeader"
                               Icon="{StaticResource IconWarning}"
                               IconForeground="{DynamicResource SystemFillColorCriticalBrush}" />

        <TextBlock x:Name="MessageText" Grid.Row="1" Margin="0,20,0,0"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   TextWrapping="Wrap" />

        <Border Grid.Row="2" Style="{StaticResource DialogFooterStyle}">
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <Button x:Name="CancelButton" Margin="0,0,8,0"
                        Style="{StaticResource DialogSecondaryButtonStyle}"
                        IsCancel="True" IsDefault="True" Click="Cancel_Click" />
                <Button Style="{StaticResource DialogSecondaryButtonStyle}"
                        Click="Confirm_Click">
                    <StackPanel Orientation="Horizontal">
                        <controls:AppIcon Width="15" Height="15" Margin="0,0,7,0"
                                          Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                                          Data="{StaticResource IconWarning}" />
                        <TextBlock x:Name="ConfirmButtonText" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
