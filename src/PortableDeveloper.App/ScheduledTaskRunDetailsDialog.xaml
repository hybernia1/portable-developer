<Window x:Class="PortableDeveloper.App.ScheduledTaskRunDetailsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="760" Height="560" MinWidth="520" MinHeight="360"
        ResizeMode="CanResizeWithGrip" ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Icon="Assets/portable-developer.ico">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeadingText" FontSize="20" FontWeight="SemiBold"
                   TextWrapping="Wrap" />
        <TextBlock x:Name="SummaryText" Grid.Row="1" Margin="0,10,0,0"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   TextWrapping="Wrap" />
        <TextBlock x:Name="OutputLabel" Grid.Row="2" Margin="0,20,0,7"
                   FontWeight="SemiBold" />
        <TextBox x:Name="OutputTextBox" Grid.Row="3" Padding="12"
                 FontFamily="Cascadia Mono,Consolas" FontSize="12"
                 IsReadOnly="True" AcceptsReturn="True" AcceptsTab="True"
                 TextWrapping="NoWrap"
                 HorizontalScrollBarVisibility="Auto"
                 VerticalScrollBarVisibility="Auto" />
        <Button x:Name="CloseButton" Grid.Row="4" Margin="0,18,0,0"
                Padding="14,9" HorizontalAlignment="Right" IsCancel="True"
                Click="Close_Click" />
    </Grid>
</Window>
