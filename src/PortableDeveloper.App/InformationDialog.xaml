<Window x:Class="PortableDeveloper.App.InformationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:PortableDeveloper.App.Controls"
        Width="520" SizeToContent="Height" MinHeight="210" MaxHeight="560"
        ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Icon="Assets/portable-developer.ico">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <controls:DialogHeader x:Name="DialogHeader"
                               Icon="{StaticResource IconInfo}" />

        <ScrollViewer Grid.Row="1" Margin="0,20,0,0" MaxHeight="330"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="MessageText"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       TextWrapping="Wrap" />
        </ScrollViewer>

        <Border Grid.Row="2" Style="{StaticResource DialogFooterStyle}">
            <Button x:Name="CloseButton" HorizontalAlignment="Right"
                    Style="{StaticResource DialogPrimaryButtonStyle}"
                    IsCancel="True" IsDefault="True"
                    Click="Close_Click" />
        </Border>
    </Grid>
</Window>
