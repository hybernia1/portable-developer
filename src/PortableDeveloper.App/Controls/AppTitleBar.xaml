<UserControl x:Class="PortableDeveloper.App.Controls.AppTitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
             x:Name="Root"
             Height="40"
             Focusable="False">
    <Border Background="{StaticResource AppTitleBarBrush}" BorderBrush="{StaticResource AppTitleBarBorderBrush}" BorderThickness="0,0,0,1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Margin="12,0,8,0" VerticalAlignment="Center" Orientation="Horizontal"
                        Visibility="{Binding ShowTitle, ElementName=Root, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Image Width="18" Height="18" Margin="0,0,9,0"
                       Source="/PortableDeveloper;component/Assets/portable-developer.png"
                       Visibility="{Binding ShowIcon, ElementName=Root, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <TextBlock VerticalAlignment="Center" FontSize="12" FontWeight="SemiBold"
                           Foreground="{StaticResource AppForegroundBrush}"
                           Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True">
                <Button x:Name="MinimizeButton"
                        Style="{StaticResource AppCaptionButtonStyle}"
                        Visibility="{Binding ShowMinimizeButton, ElementName=Root, Converter={StaticResource BooleanToVisibilityConverter}}"
                        AutomationProperties.Name="Minimize"
                        ToolTip="Minimize"
                        Click="Minimize_Click">
                    <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="10" Text="&#xE921;" />
                </Button>
                <Button x:Name="MaximizeButton"
                        Style="{StaticResource AppCaptionButtonStyle}"
                        Visibility="{Binding ShowMaximizeButton, ElementName=Root, Converter={StaticResource BooleanToVisibilityConverter}}"
                        AutomationProperties.Name="Maximize or restore"
                        ToolTip="Maximize or restore"
                        Click="Maximize_Click">
                    <TextBlock x:Name="MaximizeGlyph" FontFamily="Segoe MDL2 Assets" FontSize="10" Text="&#xE922;" />
                </Button>
                <Button Style="{StaticResource AppCloseCaptionButtonStyle}"
                        AutomationProperties.Name="Close"
                        ToolTip="Close"
                        Click="Close_Click">
                    <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="10" Text="&#xE8BB;" />
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
